<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:orientation="vertical">

    <com.example.mbankole.tripplanner.utility.RalewayLightTextView
        android:id="@+id/tvRequester"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:text="User wants to" />

    <com.example.mbankole.tripplanner.utility.RalewayLightTextView
        android:id="@+id/tvRequestBody"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/tvRequester"
        android:text="add location" />

    <Button
        android:id="@+id/btApprove"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/tvRequestBody"
        android:text="Approve" />

    <Button
        android:id="@+id/btDeny"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/btApprove"
        android:layout_toEndOf="@+id/btApprove"
        android:text="Deny" />

    <ImageView
        android:id="@+id/ivPreview"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignTop="@+id/btDeny"
        android:layout_toEndOf="@+id/btDeny"
        android:scaleType="centerCrop"
        app:srcCompat="@color/black" />
</RelativeLayout>